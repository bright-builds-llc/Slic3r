---
phase: 65-executable-wall-seam-evidence
plan: "06"
type: execute
wave: 6
depends_on: ["65-05"]
files_modified:
  - docs/port/README.md
  - docs/port/package-map.md
  - docs/port/parity-matrix.md
  - docs/port/migration-guidance.md
autonomous: true
requirements: [SEAMEV-03]
requirements_addressed: [SEAMEV-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-07-02T21-16-54
generated_at: 2026-07-02T21:32:54Z
must_haves:
  truths:
    - "Maintainer can inspect public port docs and see fork.prusaslicer.wall-seam described as the narrow checked-in wall-seam summary evidence slice."
    - "Public port docs keep broad generated-outputs in progress and keep fork.prusaslicer.gcode-output plus fork.prusaslicer.arc-fitting separate."
    - "Public port docs contain no stale Phase 65 planned/future current-state wording for wall-seam."
  artifacts:
    - path: "docs/port/parity-matrix.md"
      provides: "Generated-output and fork wall-seam status wording"
      contains: "fork.prusaslicer.wall-seam"
    - path: "docs/port/migration-guidance.md"
      provides: "Maintainer migration boundary wording for wall-seam fixtures/status"
      contains: "expected-wall-seam-summary.tsv"
    - path: "docs/port/README.md"
      provides: "Public port overview with current wall-seam evidence state"
      contains: "bazel run //packages/parity:prusaslicer_wall_seam_parity"
    - path: "docs/port/package-map.md"
      provides: "Package ownership and Phase 62-65 history wording"
      contains: "packages/prusa-wall-seam-scope"
  key_links:
    - from: "docs/port/parity-matrix.md"
      to: "packages/parity/status.tsv"
      via: "matching generated-outputs and fork.prusaslicer.wall-seam wording"
      pattern: "narrow Prusa wall-seam checked-in summary evidence slice"
    - from: "docs/port/package-map.md"
      to: "//packages/parity:prusaslicer_wall_seam_parity"
      via: "package ownership text"
      pattern: "bazel run //packages/parity:prusaslicer_wall_seam_parity"
---

<objective>
Update public port docs so they publish the exact narrow wall-seam evidence slice while keeping broad generated outputs in progress.

Purpose: Complete the public port documentation portion of SEAMEV-03 and close stale planned/future wording after Phase 65 command/status/docs publication.
Output: Updated parity matrix, migration guidance, port README, and package map for the narrow `fork.prusaslicer.wall-seam` evidence slice.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
@.planning/phases/65-executable-wall-seam-evidence/65-RESEARCH.md
@.planning/phases/65-executable-wall-seam-evidence/65-05-SUMMARY.md
@packages/parity/status.tsv
@packages/parity/README.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@docs/port/README.md
@docs/port/package-map.md
@docs/port/parity-matrix.md
@docs/port/migration-guidance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parity matrix and migration guidance for wall-seam</name>
  <files>docs/port/parity-matrix.md, docs/port/migration-guidance.md</files>
  <read_first>
    - docs/port/parity-matrix.md
    - docs/port/migration-guidance.md
    - packages/parity/status.tsv
    - packages/parity/README.md
    - packages/prusa-wall-seam-scope/wall-seam-scope.md
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <action>In `docs/port/parity-matrix.md`, update the `Generated outputs` row so it mentions `fork.prusaslicer.gcode-output`, `fork.prusaslicer.arc-fitting`, and new `fork.prusaslicer.wall-seam` as separate narrow fork evidence slices while the row status cell remains exactly `in progress`. Add `fork.prusaslicer.wall-seam` to the Fork Parity Interpretation section with this exact evidence description: `fork.prusaslicer.wall-seam` is verified in `packages/parity/status.tsv` only for the narrow Prusa wall-seam checked-in summary evidence slice through `bazel run //packages/parity:prusaslicer_wall_seam_parity`, backed by `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`, `slic3r_flavors::prusa_wall_seam`, `prusa_wall_seam_summary_lines`, the Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs. State that broad `generated-outputs` remains `in progress`, `fork.prusaslicer.gcode-output` remains separate, and `fork.prusaslicer.arc-fitting` remains separate. State that the wall-seam slice does not prove byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full wall-seam algorithm equivalence, wall-seam geometry equivalence, seam visibility, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, arc fitting behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, or non-Prusa fork behavior. In `docs/port/migration-guidance.md`, add `prusaslicer.wall-seam` to the Fixture Update Protocol: fixture work starts from `packages/prusa-wall-seam-scope`, Phase 63 created `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/`, `wall-seam-observations.gcode`, `fixture-provenance.tsv`, and `expected-wall-seam-summary.tsv`; Phase 64 owns `slic3r_flavors::prusa_wall_seam` and `prusa_wall_seam_summary_lines`; Phase 65 publishes `bazel run //packages/parity:prusaslicer_wall_seam_parity` and `fork.prusaslicer.wall-seam`. In the Future Fork Parity Status section, add the same narrow evidence slice and deferral wording. This implements D-12 and D-13 for public parity matrix and migration guidance.</action>
  <verify>
    <automated>mdformat --check docs/port/parity-matrix.md docs/port/migration-guidance.md</automated>
    <automated>bazel run //packages/parity:status</automated>
    <automated>bazel run //packages/parity:prusaslicer_wall_seam_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'narrow Prusa wall-seam checked-in summary evidence slice|Phase 62 scope contract|Phase 63 fixture corpus|Phase 64 Rust parser/readiness boundary|Phase 65 public command/status/docs' docs/port/parity-matrix.md docs/port/migration-guidance.md` returns matches in both files.
    - `rg -n 'expected-wall-seam-summary.tsv|prusa_wall_seam_summary_lines|bazel run //packages/parity:prusaslicer_wall_seam_parity|fork.prusaslicer.wall-seam' docs/port/parity-matrix.md docs/port/migration-guidance.md` returns matches.
    - `rg -n '\\| Generated outputs \\|.*\\| .*in progress.* \\|' docs/port/parity-matrix.md` returns a match.
    - `rg -n 'fork.prusaslicer.gcode-output.*fork.prusaslicer.arc-fitting.*fork.prusaslicer.wall-seam|fork.prusaslicer.wall-seam.*fork.prusaslicer.arc-fitting.*fork.prusaslicer.gcode-output|fork.prusaslicer.arc-fitting.*fork.prusaslicer.wall-seam.*fork.prusaslicer.gcode-output' docs/port/parity-matrix.md` returns a match.
    - `rg -n 'future fork\\.prusaslicer\\.wall-seam|Phase 65 planned `fork\\.prusaslicer\\.wall-seam`' docs/port/parity-matrix.md docs/port/migration-guidance.md` returns no matches.
  </acceptance_criteria>
  <done>Parity matrix and migration guidance publish the narrow wall-seam evidence slice, preserve sibling Prusa evidence slices, keep broad generated outputs in progress, and retain all deferred surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Update port README and package map ownership for wall-seam</name>
  <files>docs/port/README.md, docs/port/package-map.md</files>
  <read_first>
    - docs/port/README.md
    - docs/port/package-map.md
    - docs/port/parity-matrix.md
    - docs/port/migration-guidance.md
    - packages/parity/status.tsv
    - packages/parity/README.md
    - packages/prusa-wall-seam-scope/wall-seam-scope.md
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <action>Update `docs/port/README.md` Current Parity Visibility State with a bullet that says `bazel run //packages/parity:prusaslicer_wall_seam_parity` is the shared public command for the narrow Prusa wall-seam checked-in summary evidence slice backed by the Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs. Add `prusaslicer.wall-seam` to the fixture corpus list. Add a `## Current Prusa Wall-Seam Evidence State` section after the Prusa arc-fitting evidence state or the generated-output/fork evidence discussion. That section must name `packages/prusa-wall-seam-scope`, accepted source identity `prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, source path `src/libslic3r/GCode/SeamAligned.cpp`, fixture namespace `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/`, expected artifact `expected-wall-seam-summary.tsv`, Rust boundary `slic3r_flavors::prusa_wall_seam`, helper `prusa_wall_seam_summary_lines`, command `bazel run //packages/parity:prusaslicer_wall_seam_parity`, status row `fork.prusaslicer.wall-seam`, and evidence chain `Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs`. State that existing `fork.prusaslicer.gcode-output` and `fork.prusaslicer.arc-fitting` remain separate and broad `generated-outputs` remains `in progress`. Include the D-10/D-12 deferral list: byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full wall-seam algorithm equivalence, wall-seam geometry equivalence, seam visibility, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, arc fitting behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, and non-Prusa fork behavior. Update `docs/port/package-map.md` so `packages/slic3r-rust` owns the Phase 64 `slic3r_flavors::prusa_wall_seam` parser/readiness boundary and Phase 65 summary adapter, `packages/parity` owns `bazel run //packages/parity:prusaslicer_wall_seam_parity` and the `fork.prusaslicer.wall-seam` status row, `packages/parity-fixtures` owns the Phase 63 Prusa wall-seam fixture namespace and `expected-wall-seam-summary.tsv`, and a new `packages/prusa-wall-seam-scope` package row owns the Phase 62 scope contract and verifier. Add Phase 62, 63, 64, and 65 note bullets for the wall-seam chain, using the same exact phase-chain wording. This is the final public port docs step in the D-14/D-15 sequence after scope docs pass; do not add broad generated-output verification, release, sync, launcher, packaging, network/device, host upload, upstream import, or non-Prusa support claims per D-17. This implements D-12 and D-13 for public README/package ownership docs.</action>
  <verify>
    <automated>mdformat --check docs/port/README.md docs/port/package-map.md</automated>
    <automated>bazel run //packages/parity:prusaslicer_wall_seam_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Current Prusa Wall-Seam Evidence State|narrow Prusa wall-seam checked-in summary evidence slice|Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs' docs/port/README.md` returns matches.
    - `rg -n 'packages/prusa-wall-seam-scope|slic3r_flavors::prusa_wall_seam|expected-wall-seam-summary.tsv|bazel run //packages/parity:prusaslicer_wall_seam_parity|fork.prusaslicer.wall-seam' docs/port/package-map.md` returns matches.
    - `rg -n 'fork.prusaslicer.gcode-output.*separate|fork.prusaslicer.arc-fitting.*separate|generated-outputs.*in progress|in progress.*generated-outputs' docs/port/README.md docs/port/package-map.md` returns matches.
    - `rg -n 'future fork\\.prusaslicer\\.wall-seam|Phase 65 planned `fork\\.prusaslicer\\.wall-seam`|planned public evidence command' docs/port/README.md docs/port/package-map.md` returns no matches.
  </acceptance_criteria>
  <done>Port README and package map publish wall-seam current-state ownership, include the Phase 62-65 chain, keep sibling Prusa rows separate, and keep broad generated-output status in progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public port docs -> maintainer decisions | Port docs communicate what evidence is verified and what remains deferred. |
| status TSV -> port docs | Port docs must match `packages/parity/status.tsv` without promoting broad generated-output status. |
| package ownership docs -> future executors | Package map guides future changes to the correct local checked-in packages. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification Criteria |
|-----------|----------|----------|-----------|-------------|-----------------|-----------------------|
| T-65-06-01 | Spoofing | High | public port evidence wording | mitigate | Use exact status token, public command, and Phase 62-65 wall-seam chain in all touched docs. | `rg` acceptance confirms command, row, artifact, and phase-chain wording. |
| T-65-06-02 | Tampering | High | broad generated-output status | mitigate | Require `Generated outputs` to remain `in progress` and keep `fork.prusaslicer.gcode-output` plus `fork.prusaslicer.arc-fitting` separate from `fork.prusaslicer.wall-seam`. | Acceptance grep confirms `in progress` status and sibling-row separation; `bazel run //packages/parity:status` passes. |
| T-65-06-03 | Repudiation | High | stale planned/future wording | mitigate | Add grep acceptance checks that reject specific stale Phase 65 planned/future wall-seam wording in touched port docs. | Acceptance grep for stale strings returns no matches. |
| T-65-06-04 | Information Disclosure | Medium | docs-only publication | mitigate | Docs introduce no secrets, credentials, private data, file discovery, network/device behavior, upstream source import, host upload, runtime discovery, sync behavior, release behavior, or printer-runtime exposure. | Deferral list remains explicit in touched public docs. |
</threat_model>

<verification>
Run after both tasks:

```bash
mdformat --check docs/port/README.md docs/port/package-map.md docs/port/parity-matrix.md docs/port/migration-guidance.md
bazel run //packages/parity:status
bazel run //packages/parity:prusaslicer_wall_seam_parity
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel run //packages/parity:prusaslicer_arc_fitting_parity
bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture
bazel run //packages/prusa-wall-seam-scope:verify
awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv
git diff --check -- docs/port/README.md docs/port/package-map.md docs/port/parity-matrix.md docs/port/migration-guidance.md
```
</verification>

<success_criteria>
SEAMEV-03 public port docs are complete when the parity matrix, migration guidance, port README, and package map all describe `fork.prusaslicer.wall-seam` as the narrow checked-in wall-seam summary evidence slice, keep broad `generated-outputs` in progress, keep sibling Prusa rows separate, and contain no stale planned/future current-state wording.
</success_criteria>

<output>
After completion, create `.planning/phases/65-executable-wall-seam-evidence/65-06-SUMMARY.md`.
</output>
