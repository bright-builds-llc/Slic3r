---
phase: 65-executable-wall-seam-evidence
plan: "04"
type: execute
wave: 4
depends_on: ["65-03"]
files_modified:
  - packages/parity/README.md
  - packages/parity-fixtures/README.md
  - packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md
  - packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
  - packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
autonomous: true
requirements: [SEAMEV-03]
requirements_addressed: [SEAMEV-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-07-02T21-16-54
generated_at: 2026-07-02T21:32:54Z
must_haves:
  truths:
    - "Maintainer can read package docs for the public wall-seam command and status row."
    - "Fixture docs say Phase 65 published the command/status row and still keep the evidence boundary checked-in-summary-only."
    - "Fixture verifier exact-text checks reject stale Phase 65 owns/planned wording."
  artifacts:
    - path: "packages/parity/README.md"
      provides: "Parity package docs for prusaslicer_wall_seam_parity"
      contains: "bazel run //packages/parity:prusaslicer_wall_seam_parity"
    - path: "packages/parity-fixtures/README.md"
      provides: "Fixture package docs for Phase 65 wall-seam publication"
      contains: "Phase 65 publishes"
    - path: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      provides: "Exact fixture doc checks for published Phase 65 wording"
      contains: "Phase 65 publishes"
  key_links:
    - from: "packages/parity/README.md"
      to: "packages/parity/status.tsv"
      via: "fork.prusaslicer.wall-seam docs section"
      pattern: "fork\\.prusaslicer\\.wall-seam"
    - from: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      to: "packages/parity-fixtures/README.md"
      via: "require_text exact docs checks"
      pattern: "Phase 65 publishes"
---

<objective>
Publish package-local wall-seam documentation and align fixture verifier exact-text checks.

Purpose: SEAMEV-03 requires package docs to expose the new command/status row without implying byte parity, generated-output parity, wall-seam behavior, geometry equivalence, printability, runtime, GUI, release, sync, host upload, network/device, or non-Prusa behavior.
Output: Updated `packages/parity` and `packages/parity-fixtures` docs plus fixture verifier/test checks for the published Phase 65 wording.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
@.planning/phases/65-executable-wall-seam-evidence/65-RESEARCH.md
@.planning/phases/65-executable-wall-seam-evidence/65-03-SUMMARY.md
@packages/parity/README.md
@packages/parity-fixtures/README.md
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md
@packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
@packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish parity and fixture package docs for wall-seam</name>
  <files>packages/parity/README.md, packages/parity-fixtures/README.md, packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md</files>
  <read_first>
    - packages/parity/README.md
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md
    - packages/parity/status.tsv
    - packages/parity/compare_prusaslicer_wall_seam.sh
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <action>Update `packages/parity/README.md` Current Surface with this command text: `` `bazel run //packages/parity:prusaslicer_wall_seam_parity` runs the shared fixture comparison for the narrow Prusa wall-seam checked-in summary evidence slice backed by `expected-wall-seam-summary.tsv`. Public proof: checked-in wall-seam summary evidence only.`` Add a Fork Status Rows section for `fork.prusaslicer.wall-seam` that says exactly these facts: command `bazel run //packages/parity:prusaslicer_wall_seam_parity`; checked artifact `packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`; Rust boundary `slic3r_flavors::prusa_wall_seam` and helper `prusa_wall_seam_summary_lines`; evidence chain `Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs`; `generated-outputs` remains `in progress`; existing semantic Prusa G-code output evidence remains separate under `fork.prusaslicer.gcode-output`; existing Prusa arc-fitting evidence remains separate under `fork.prusaslicer.arc-fitting`. Update `packages/parity-fixtures/README.md` and the fixture-local README to replace future/owned wording with this exact sentence: `Phase 65 publishes bazel run //packages/parity:prusaslicer_wall_seam_parity and the fork.prusaslicer.wall-seam status row for checked-in wall-seam summary evidence only.` Keep the existing `wall-seam-observations.gcode`, `fixture-provenance.tsv`, `expected-wall-seam-summary.tsv`, source ref, source path, and Phase 62/63/64 chain text. Add or keep explicit deferrals for byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full wall-seam algorithm equivalence, wall-seam geometry equivalence, seam visibility, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, arc fitting behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, and non-Prusa fork behavior. This is the package/fixture docs step in the D-14/D-15 sequence after 65-03 status/verifier guards pass; do not edit scope docs or public port docs in this plan. This implements D-11 and the package/fixture docs subset of D-12/D-13.</action>
  <verify>
    <automated>mdformat --check packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md</automated>
    <automated>bazel run //packages/parity:prusaslicer_wall_seam_parity</automated>
    <automated>bazel run //packages/parity:status</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'bazel run //packages/parity:prusaslicer_wall_seam_parity' packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md` returns matches in all three files.
    - `rg -n 'fork.prusaslicer.wall-seam' packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md` returns matches in all three files.
    - `rg -n 'Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs' packages/parity/README.md` returns a match.
    - `rg -n 'existing semantic Prusa G-code output evidence remains separate|existing Prusa arc-fitting evidence remains separate' packages/parity/README.md` returns matches.
    - `rg -n 'generated-outputs.*in progress|in progress.*generated-outputs' packages/parity/README.md packages/parity-fixtures/README.md` returns matches.
  </acceptance_criteria>
  <done>Package docs expose the wall-seam command/status row, name the Phase 62-65 chain, preserve sibling Prusa row meanings, and retain all deferrals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align fixture verifier exact-text checks with published package docs</name>
  <files>packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh, packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh</files>
  <read_first>
    - packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
    - packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
    - packages/parity-fixtures/README.md
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md
    - packages/parity/status.tsv
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <behavior>
    - RED: The fixture verifier still requires or accepts Phase 65 owned/future wording before this task.
    - GREEN: The fixture verifier requires published Phase 65 command/status wording and rejects stale owned/future wording.
  </behavior>
  <action>Update `verify_prusa_wall_seam_fixture.sh` so `verify_namespace_readme` and `verify_package_readme` require the exact published substring `Phase 65 publishes bazel run //packages/parity:prusaslicer_wall_seam_parity and the fork.prusaslicer.wall-seam status row for checked-in wall-seam summary evidence only.` Remove requirements that demand `Phase 65 owns`, `planned`, or future `fork.prusaslicer.wall-seam` wording in package/fixture docs. Keep requirements for `Phase 63`, `Phase 64 owns \`slic3r_flavors::prusa_wall_seam\``, `expected-wall-seam-summary.tsv`, source ref, source path, fixture path, `generated-outputs` in progress, and sibling row separation. Update `verify_prusa_wall_seam_fixture_test.sh` valid README fixtures to the new text. Add one stale-doc mutation case that replaces the exact Phase 65 published sentence with `Phase 65 owns future bazel run //packages/parity:prusaslicer_wall_seam_parity evidence and the planned fork.prusaslicer.wall-seam status row.` and assert the diagnostic mentions `Phase 65 publishes`. Keep existing mutation coverage for fixture bytes, source/fixture identity, row order, status rows, local-only behavior, and overclaiming text.</action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh</automated>
    <automated>shfmt -l -d packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture</automated>
    <automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Phase 65 publishes bazel run //packages/parity:prusaslicer_wall_seam_parity and the fork.prusaslicer.wall-seam status row for checked-in wall-seam summary evidence only\\.' packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh` returns matches in both files.
    - `rg -n 'future `fork.prusaslicer.wall-seam` status row|Phase 65 owns future|Phase 65 owns `bazel run //packages/parity:prusaslicer_wall_seam_parity`' packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh` returns no matches.
    - `bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture` exits 0.
    - `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <done>Fixture verifier exact-text checks enforce the published package docs and reject stale Phase 65 owned/future-status wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package docs -> maintainer interpretation | Docs become public evidence guidance. |
| fixture README -> fixture verifier | Exact verifier checks decide whether package docs are stale or overclaiming. |
| status row -> docs | Package docs must describe the exact checked-in status row without widening it. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification Criteria |
|-----------|----------|----------|-----------|-------------|-----------------|-----------------------|
| T-65-04-01 | Spoofing | High | package docs | mitigate | Docs name only the checked-in wall-seam summary evidence slice and exact command/status row. | `rg` acceptance confirms command, status token, and Phase 62-65 chain across touched docs. |
| T-65-04-02 | Repudiation | High | fixture docs | mitigate | Fixture verifier requires exact Phase 65 publication wording and mutation tests reject stale future/owned wording. | `bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture` and its mutation test pass. |
| T-65-04-03 | Tampering | High | docs/status drift | mitigate | Keep `generated-outputs` in progress and explicitly state G-code output and arc-fitting rows remain separate. | Acceptance grep verifies generated-output and sibling-row separation wording. |
| T-65-04-04 | Information Disclosure | Medium | docs | mitigate | Docs introduce no secrets, credentials, private data, file discovery, network/device behavior, upstream source import, host upload, sync behavior, release behavior, or printer-runtime exposure. | Deferral wording remains present in package docs and verifier checks. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel test //packages/parity:prusaslicer_wall_seam_parity_failure_test --test_output=errors
git diff --check -- packages/parity/README.md packages/parity-fixtures/README.md packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/README.md packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
```
</verification>

<success_criteria>
SEAMEV-03 package documentation is satisfied when `packages/parity` and `packages/parity-fixtures` expose the public wall-seam command/status row, keep sibling Prusa rows separate, preserve deferrals, and the fixture verifier enforces that wording.
</success_criteria>

<output>
After completion, create `.planning/phases/65-executable-wall-seam-evidence/65-04-SUMMARY.md`.
</output>
