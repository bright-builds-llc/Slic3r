---
phase: 65-executable-wall-seam-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs
  - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
  - packages/parity/compare_prusaslicer_wall_seam.sh
  - packages/parity/BUILD.bazel
autonomous: true
requirements: [SEAMEV-01]
requirements_addressed: [SEAMEV-01]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-07-02T21-16-54
generated_at: 2026-07-02T21:32:54Z
must_haves:
  truths:
    - "Maintainer can run bazel run //packages/parity:prusaslicer_wall_seam_parity."
    - "The public command validates expected-wall-seam-summary.tsv through slic3r_flavors::prusa_wall_seam_summary_lines."
    - "Existing public Prusa G-code output and arc-fitting commands still pass unchanged."
  artifacts:
    - path: "packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs"
      provides: "Thin Rust CLI adapter over prusa_wall_seam_summary_lines"
      contains: "prusa_wall_seam_summary_lines"
    - path: "packages/parity/compare_prusaslicer_wall_seam.sh"
      provides: "Public shell comparator for checked-in wall-seam summary evidence"
      contains: "ok: fork.prusaslicer.wall-seam checked-in summary evidence passed"
    - path: "packages/parity/BUILD.bazel"
      provides: "prusaslicer_wall_seam_parity sh_binary target"
      contains: "name = \"prusaslicer_wall_seam_parity\""
  key_links:
    - from: "packages/parity/BUILD.bazel"
      to: "packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary"
      via: "sh_binary data and args"
      pattern: "prusa_wall_seam_summary"
    - from: "packages/parity/compare_prusaslicer_wall_seam.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv"
      via: "explicit Bazel runfile argument"
      pattern: "expected-wall-seam-summary.tsv"
---

<objective>
Add the Rust-backed public wall-seam evidence command required by SEAMEV-01.

Purpose: Phase 65 must publish a runnable wall-seam evidence command without widening the existing public Prusa G-code output command, the arc-fitting command, or broad `generated-outputs`.
Output: Rust binary `//packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary`, shell comparator `packages/parity/compare_prusaslicer_wall_seam.sh`, and public target `//packages/parity:prusaslicer_wall_seam_parity`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
@.planning/phases/65-executable-wall-seam-evidence/65-RESEARCH.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-01-SUMMARY.md
@.planning/phases/64-rust-wall-seam-evidence-boundary/64-02-SUMMARY.md
@packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs
@packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs
@packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
@packages/parity/compare_prusaslicer_arc_fitting.sh
@packages/parity/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv

<interfaces>
From `packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs`:
```rust
pub use prusa_wall_seam::{
    parse_prusa_wall_seam_summary,
    prusa_wall_seam_summary_lines,
};
```

`prusa_wall_seam_summary_lines(input: &str)` returns these exact keys:
`wall_seam_summary_path`, `wall_seam_row_count`, `source_ref`, `inventory_source_paths`, `source_anchor`, `fixture_id`, `fixture_path`, `seam_transition_observations`, `layer_context_observations`, `travel_context_observations`, `coordinate_bounds`, `extrusion_observations`, `retraction_observations`, and `evidence_boundary`.

Required public facts per D-03:
`source_ref=prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `wall_seam_row_count=12`, `fixture_id=wall-seam-observations.gcode`, `fixture_path=packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode`, `seam_transition_observations=seam_markers:seam_start,seam_resume;transition_count:2`, `layer_context_observations=layer_index:0;z_values:0.200`, `travel_context_observations=travel_moves:1;travel_from:12.500,8.000;travel_to:14.000,8.750`, `coordinate_bounds=x_min:12.500;x_max:15.250;y_min:8.000;y_max:9.500;z_min:0.200;z_max:0.200`, `extrusion_observations=e_values:0.12000,0.28000;e_axis_observed:true`, `retraction_observations=e_marker_values:0.28000,0.24000;retraction_marker_observed:true`, and `evidence_boundary=checked-in-wall-seam-summary-only`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the prusa_wall_seam_summary Rust binary</name>
  <files>packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs, packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel</files>
  <read_first>
    - NEW FILE: packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_arc_fitting_summary.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs
    - packages/slic3r-rust/crates/slic3r_flavors/src/lib.rs
    - packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <behavior>
    - RED: `bazel query //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary` fails before the target exists.
    - GREEN: Running the binary with one `expected-wall-seam-summary.tsv` path prints Rust-produced summary lines and exits 0.
    - Error behavior: wrong arity exits nonzero and prints `error: expected expected-wall-seam-summary.tsv`.
  </behavior>
  <action>Create `packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs` with `#![forbid(unsafe_code)]`. Mirror `prusa_arc_fitting_summary.rs` per D-01: use `std::{env, fs, path::Path, process::ExitCode}` and import `slic3r_flavors::prusa_wall_seam_summary_lines`. Accept exactly one argument named by the error text `expected-wall-seam-summary.tsv`. On wrong arity, return `Err("expected expected-wall-seam-summary.tsv".to_owned())`. Read the caller-supplied local file with `fs::read_to_string(path)` and map read errors to `failed to read {path}: {error}`. Call `prusa_wall_seam_summary_lines(&input)` and map parser errors to `failed to summarize wall-seam {path}: {error:?}`. Print every returned line with `println!("{line}")`. In `BUILD.bazel`, add `rust_binary(name = "prusa_wall_seam_summary", srcs = ["src/bin/prusa_wall_seam_summary.rs"], crate_root = "src/bin/prusa_wall_seam_summary.rs", deps = [":slic3r_flavors"], edition = "2024")`, then add `:prusa_wall_seam_summary` to both `rust_clippy(name = "clippy")` deps and `rustfmt_test(name = "rustfmt_check")` targets. Do not edit `prusa_arc_fitting_summary.rs` or `prusa_gcode_output_summary.rs`, per D-04. Because this task changes Rust/Bazel wiring, keep Rust 1.94.1 Cargo workspace verification in this plan per D-16 and do not ask the executor to run broad legacy, launcher, packaging, release, sync, or external network suites per D-17.</action>
  <verify>
    <automated>bazel query //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary</automated>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary</automated>
    <automated>bazel test //packages/slic3r-rust/crates/slic3r_flavors:rustfmt_check --test_output=errors</automated>
    <automated>bazel build //packages/slic3r-rust/crates/slic3r_flavors:clippy</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "prusa_wall_seam_summary"' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` returns a match.
    - `rg -n 'prusa_wall_seam_summary_lines' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs` returns a match.
    - `rg -n 'expected expected-wall-seam-summary.tsv' packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs` returns a match.
    - `rg -n ':prusa_wall_seam_summary' packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel` returns at least two matches covering clippy deps and rustfmt targets.
  </acceptance_criteria>
  <done>`prusa_wall_seam_summary` exists as a Bazel Rust binary, reads only an explicit local TSV path, delegates parsing to `prusa_wall_seam_summary_lines`, and is covered by crate clippy/rustfmt wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the public wall-seam parity command</name>
  <files>packages/parity/compare_prusaslicer_wall_seam.sh, packages/parity/BUILD.bazel</files>
  <read_first>
    - NEW FILE: packages/parity/compare_prusaslicer_wall_seam.sh
    - packages/parity/compare_prusaslicer_arc_fitting.sh
    - packages/parity/compare_prusaslicer_gcode_output.sh
    - packages/parity/BUILD.bazel
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/fixture-provenance.tsv
    - packages/slic3r-rust/crates/slic3r_flavors/src/prusa_wall_seam.rs
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <behavior>
    - RED: `bazel query //packages/parity:prusaslicer_wall_seam_parity` fails before the target exists.
    - GREEN: `bazel run //packages/parity:prusaslicer_wall_seam_parity` exits 0 and prints the exact narrow wall-seam facts listed below.
    - Regression: `bazel run //packages/parity:prusaslicer_gcode_output_parity` and `bazel run //packages/parity:prusaslicer_arc_fitting_parity` still exit 0.
  </behavior>
  <action>Create `packages/parity/compare_prusaslicer_wall_seam.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`, per D-02. Accept exactly four arguments: `summary_binary rust-wall-seam-input.tsv expected-wall-seam-summary.tsv fixture-provenance.tsv`. Require all four paths to exist and require `summary_binary` to be executable. Run the Rust binary once against `rust-wall-seam-input.tsv` into `actual-wall-seam-summary.tsv` and once against `expected-wall-seam-summary.tsv` into `expected-wall-seam-summary-lines.tsv`, then `diff -u expected-wall-seam-summary-lines.tsv actual-wall-seam-summary.tsv`. On Rust validation failure for the expected artifact, print `error: expected-wall-seam-summary.tsv failed Rust wall-seam validation at {field} in {path}` and include a `diff -u` against the Rust input when possible. On summary mismatch, print `error: expected-wall-seam-summary.tsv mismatch at {field} in {path}` and include `diff -u`. Add `field_value`, `assert_field`, `first_wall_seam_raw_mismatch_label`, and `first_wall_seam_summary_mismatch_label` helpers equivalent to `compare_prusaslicer_arc_fitting.sh` but keyed to wall-seam fields. Assert exact values from the `<interfaces>` block, including `wall_seam_summary_path=packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv` and `wall_seam_row_count=12`. Print exactly these public lines: `ok: fork.prusaslicer.wall-seam checked-in summary evidence passed`, `source_ref: prusaslicer:version_2.9.5@9a583bd438b195856f3bcf7ea99b69ba4003a961`, `fixture: packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/wall-seam-observations.gcode`, `expected_wall_seam_summary: packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv`, `wall_seam_rows: 12`, `seam_transition_observations: seam_markers:seam_start,seam_resume;transition_count:2`, `layer_context_observations: layer_index:0;z_values:0.200`, `travel_context_observations: travel_moves:1;travel_from:12.500,8.000;travel_to:14.000,8.750`, `coordinate_bounds: x_min:12.500;x_max:15.250;y_min:8.000;y_max:9.500;z_min:0.200;z_max:0.200`, `extrusion_observations: e_values:0.12000,0.28000;e_axis_observed:true`, `retraction_observations: e_marker_values:0.28000,0.24000;retraction_marker_observed:true`, and `evidence_boundary: checked-in-wall-seam-summary-only`, per D-03. In `packages/parity/BUILD.bazel`, add `sh_binary(name = "prusaslicer_wall_seam_parity", srcs = ["compare_prusaslicer_wall_seam.sh"], data = ["status.tsv", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary", "//packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary", "//packages/parity-fixtures:prusa_wall_seam_provenance"], args = ["$(location //packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary)", "$(location //packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary)", "$(location //packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary)", "$(location //packages/parity-fixtures:prusa_wall_seam_provenance)"])`. Do not refactor or edit existing G-code output or arc-fitting comparators, per D-04.</action>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_wall_seam.sh</automated>
    <automated>shfmt -l -d packages/parity/compare_prusaslicer_wall_seam.sh</automated>
    <automated>bazel query //packages/parity:prusaslicer_wall_seam_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_wall_seam_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "prusaslicer_wall_seam_parity"' packages/parity/BUILD.bazel` returns a match.
    - `rg -n 'compare_prusaslicer_wall_seam.sh' packages/parity/BUILD.bazel packages/parity/compare_prusaslicer_wall_seam.sh` returns matches.
    - `rg -n 'ok: fork.prusaslicer.wall-seam checked-in summary evidence passed' packages/parity/compare_prusaslicer_wall_seam.sh` returns a match.
    - `rg -n 'seam_transition_observations: seam_markers:seam_start,seam_resume;transition_count:2' packages/parity/compare_prusaslicer_wall_seam.sh` returns a match.
    - `rg -n 'evidence_boundary: checked-in-wall-seam-summary-only' packages/parity/compare_prusaslicer_wall_seam.sh` returns a match.
    - `rg -n 'wall-seam behavior verified|geometry equivalence verified|printability verified|printer-runtime behavior verified|GUI behavior verified|release behavior verified|sync automation verified|non-Prusa fork behavior verified' packages/parity/compare_prusaslicer_wall_seam.sh` returns no matches.
  </acceptance_criteria>
  <done>The public wall-seam command validates the checked-in wall-seam summary through Rust, prints only the approved wall-seam facts, and leaves existing Prusa G-code output and arc-fitting command contracts unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in TSV -> Rust parser | `expected-wall-seam-summary.tsv` is parsed as local evidence input before public output is produced. |
| Bazel runfiles -> shell comparator | Shell receives explicit Bazel runfile paths and must not discover runtime files or external sources. |
| command output -> maintainer interpretation | Public wording must not overclaim wall-seam behavior, byte parity, geometry equivalence, printability, runtime, GUI, release, sync, or fork support. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification Criteria |
|-----------|----------|----------|-----------|-------------|-----------------|-----------------------|
| T-65-01-01 | Tampering | High | `prusa_wall_seam_summary.rs` and comparator | mitigate | Validate TSV through `prusa_wall_seam_summary_lines`, assert exact fields, and diff Rust-produced summary lines. | `bazel run //packages/parity:prusaslicer_wall_seam_parity` exits 0; mutation plan 65-02 proves drift fails. |
| T-65-01-02 | Elevation of Privilege / Information Disclosure | High | shell comparator path handling | mitigate | Use explicit Bazel runfile arguments, require local files, and avoid Git, network, source import, runtime discovery, host upload, printer-runtime, or device behavior. | `rg -n 'curl |git |clone|fetch|host upload|send-gcode|PrusaSlicer --|slic3r --' packages/parity/compare_prusaslicer_wall_seam.sh` returns no matches except split literal guards if added. |
| T-65-01-03 | Spoofing / Repudiation | High | public command wording | mitigate | Print only exact checked-in summary facts and the `checked-in-wall-seam-summary-only` boundary. | Acceptance grep rejects verified behavior, geometry, printability, runtime, GUI, release, sync, and non-Prusa support claims. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
rustup run 1.94.1 cargo test --manifest-path packages/slic3r-rust/Cargo.toml --all-features
bazel test //packages/slic3r-rust:verify --test_output=errors
git diff --check -- packages/slic3r-rust/crates/slic3r_flavors/src/bin/prusa_wall_seam_summary.rs packages/slic3r-rust/crates/slic3r_flavors/BUILD.bazel packages/parity/compare_prusaslicer_wall_seam.sh packages/parity/BUILD.bazel
```
</verification>

<success_criteria>
SEAMEV-01 is satisfied for the command layer when `bazel run //packages/parity:prusaslicer_wall_seam_parity` passes, prints the approved wall-seam facts, uses the Rust boundary, and both existing Prusa public generated-output commands still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/65-executable-wall-seam-evidence/65-01-SUMMARY.md`.
</output>
