---
phase: 65-executable-wall-seam-evidence
plan: "03"
type: execute
wave: 3
depends_on: ["65-01", "65-02"]
files_modified:
  - packages/parity/status.tsv
  - packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
  - packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
  - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
  - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
autonomous: true
requirements: [SEAMEV-03]
requirements_addressed: [SEAMEV-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-07-02T21-16-54
generated_at: 2026-07-02T21:32:54Z
must_haves:
  truths:
    - "Maintainer can inspect exactly one fork.prusaslicer.wall-seam verified row in packages/parity/status.tsv."
    - "Broad generated-outputs remains exactly one in progress row."
    - "Existing fork.prusaslicer.gcode-output and fork.prusaslicer.arc-fitting status wording remains unchanged."
  artifacts:
    - path: "packages/parity/status.tsv"
      provides: "Published narrow wall-seam status row"
      contains: "fork.prusaslicer.wall-seam\tverified\t//packages/parity:prusaslicer_wall_seam_parity"
    - path: "packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh"
      provides: "Fixture verifier status-row publication enforcement"
      contains: "WALL_SEAM_STATUS_ROW"
    - path: "packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh"
      provides: "Scope verifier status-row publication enforcement"
      contains: "WALL_SEAM_STATUS_ROW"
  key_links:
    - from: "packages/parity/status.tsv"
      to: "//packages/parity:prusaslicer_wall_seam_parity"
      via: "status evidence column"
      pattern: "prusaslicer_wall_seam_parity"
    - from: "packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh"
      to: "packages/parity/status.tsv"
      via: "exact WALL_SEAM_STATUS_ROW check"
      pattern: "fork\\.prusaslicer\\.wall-seam"
---

<objective>
Publish the exact narrow `fork.prusaslicer.wall-seam` status row and update fail-closed status verifiers.

Purpose: SEAMEV-03 requires public status publication only after the public command and mutation guards pass, while preserving broad `generated-outputs`, existing semantic G-code output, and existing arc-fitting meanings.
Output: One new exact status row and verifier/test updates that reject row drift, missing rows, duplicate rows, broad generated-output promotion, and sibling Prusa row widening.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
@.planning/phases/65-executable-wall-seam-evidence/65-RESEARCH.md
@.planning/phases/65-executable-wall-seam-evidence/65-01-SUMMARY.md
@.planning/phases/65-executable-wall-seam-evidence/65-02-SUMMARY.md
@packages/parity/status.tsv
@packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
@packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
@packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
@packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh

<interfaces>
Exact status row to add and enforce:
```text
fork.prusaslicer.wall-seam	verified	//packages/parity:prusaslicer_wall_seam_parity	Shared fixture comparison proves the narrow Prusa wall-seam checked-in summary evidence slice backed by the Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public command/status/docs only; byte-for-byte G-code parity, full generated-output parity, broad generated-output verification, full wall-seam algorithm equivalence, wall-seam geometry equivalence, seam visibility, printability, firmware behavior, printer-runtime behavior, GUI behavior, support generation, arc fitting behavior, STEP import, full 3MF import/export, binary G-code, thumbnails, post-processing, host upload, network/device behavior, profile auto-update execution, fork release builds, Bambu Studio, OrcaSlicer, upstream source imports, release behavior, sync automation, and non-Prusa fork behavior remain deferred
```

Existing `generated-outputs` must remain exactly one row with status `in progress`.
Existing `fork.prusaslicer.gcode-output` and `fork.prusaslicer.arc-fitting` rows must remain exact; do not add wall-seam claims to either row, per D-10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish and enforce the exact wall-seam status row</name>
  <files>packages/parity/status.tsv, packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh, packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh</files>
  <read_first>
    - packages/parity/status.tsv
    - packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
    - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
    - packages/parity/compare_prusaslicer_wall_seam.sh
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <action>Add the exact `fork.prusaslicer.wall-seam` row shown in the interfaces block to `packages/parity/status.tsv` immediately after `fork.prusaslicer.arc-fitting`. Do not edit the existing `generated-outputs`, `fork.prusaslicer.gcode-output`, or `fork.prusaslicer.arc-fitting` row text, per D-10. In both `verify_prusa_wall_seam_fixture.sh` and `verify_prusa_wall_seam_scope.sh`, add `readonly WALL_SEAM_STATUS_ROW=$'...'` using the exact row text above. Replace the current Phase 62/63 absence checks (`wall_seam_status_count != 0`) with: require exact `WALL_SEAM_STATUS_ROW` once, require first field count `fork.prusaslicer.wall-seam` equals `1`, require exactly one `generated-outputs` row with status `in progress`, require exactly one unchanged `fork.prusaslicer.gcode-output` row, and require exactly one unchanged `fork.prusaslicer.arc-fitting` row. Preserve all forbidden-claim checks, local-only behavior checks, split self-scan literals, and generated-output guards. This is the status/verifier publication step in the D-14/D-15 sequence after 65-01 and 65-02 pass; do not publish docs before these exact status guards exist. This implements D-09, D-10, and the status subset of D-13.</action>
  <verify>
    <automated>bazel run //packages/parity:status</automated>
    <automated>bazel run //packages/parity:prusaslicer_wall_seam_parity</automated>
    <automated>bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture</automated>
    <automated>bazel run //packages/prusa-wall-seam-scope:verify</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F '\t' '$1=="fork.prusaslicer.wall-seam" && $2=="verified" && $3=="//packages/parity:prusaslicer_wall_seam_parity" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="generated-outputs" && $2=="in progress" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="fork.prusaslicer.gcode-output" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `awk -F '\t' '$1=="fork.prusaslicer.arc-fitting" { count++ } END { exit count == 1 ? 0 : 1 }' packages/parity/status.tsv` exits 0.
    - `rg -n 'WALL_SEAM_STATUS_ROW' packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` returns matches in both files.
  </acceptance_criteria>
  <done>Status publication is exact, local verifiers require the new row, broad `generated-outputs` remains in progress, and the existing G-code output and arc-fitting rows are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add status-row mutation coverage for fixture and scope verifiers</name>
  <files>packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh, packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh</files>
  <read_first>
    - packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh
    - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
    - packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh
    - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
    - packages/parity/status.tsv
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <behavior>
    - RED: Existing mutation suites still expect premature wall-seam status publication to fail before this task.
    - GREEN: Fixture and scope verifier tests reject missing wall-seam row, duplicate wall-seam row, wrong evidence target, broad generated-output promotion, G-code row drift, and arc-fitting row drift.
  </behavior>
  <action>Update both wall-seam verifier mutation test scripts so their valid status fixtures include exactly one `generated-outputs	in progress` row, exactly one unchanged `fork.prusaslicer.gcode-output` row, exactly one unchanged `fork.prusaslicer.arc-fitting` row, and exactly one exact `fork.prusaslicer.wall-seam	verified	//packages/parity:prusaslicer_wall_seam_parity` row with the row text from this plan. Replace the old `test_premature_wall_seam_status_publication_fails` expectations with published-state tests. Add focused failure cases in both test files: remove the wall-seam row and assert the diagnostic mentions `fork.prusaslicer.wall-seam`; duplicate the wall-seam row and assert the diagnostic mentions `fork.prusaslicer.wall-seam`; change the evidence target to `//packages/parity:prusaslicer_arc_fitting_parity` and assert the diagnostic mentions `//packages/parity:prusaslicer_wall_seam_parity`; change `generated-outputs` status from `in progress` to `verified` and assert the diagnostic mentions `generated-outputs`; mutate the existing G-code row by replacing `narrow semantic Prusa G-code evidence slice` with `narrow semantic Prusa G-code and wall-seam evidence slice` and assert the diagnostic mentions `fork.prusaslicer.gcode-output`; mutate the existing arc-fitting row by replacing `narrow Prusa arc-fitting checked-in summary evidence slice` with `narrow Prusa arc-fitting and wall-seam checked-in summary evidence slice` and assert the diagnostic mentions `fork.prusaslicer.arc-fitting`. Keep each test case as an isolated temp fixture mutation and preserve existing checksum, source, field, row-order, local-only, and forbidden-claim mutation coverage.</action>
  <verify>
    <automated>bash -n packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh</automated>
    <automated>shfmt -l -d packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh</automated>
    <automated>bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'fork.prusaslicer.wall-seam' packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` returns matches in both files.
    - `rg -n 'prusaslicer_wall_seam_parity' packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` returns matches in both files.
    - `rg -n 'generated-outputs' packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` returns matches in both files.
    - `rg -n 'gcode-output|arc-fitting' packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` returns matches in both files.
    - `bazel test --cache_test_results=no //packages/parity-fixtures:verify_prusa_wall_seam_fixture_test //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <done>Fixture and scope verifier mutation suites fail closed on wall-seam status absence, duplication, wrong target, broad generated-output promotion, and sibling Prusa row widening.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status.tsv -> public parity status | Checked-in public status text becomes maintainer-facing evidence. |
| status.tsv -> verifier scripts | Verifiers enforce exact publication state. |
| test temp status fixtures -> verifier diagnostics | Mutation tests prove false or widened status claims fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification Criteria |
|-----------|----------|----------|-----------|-------------|-----------------|-----------------------|
| T-65-03-01 | Spoofing | High | `fork.prusaslicer.wall-seam` row | mitigate | Add exactly one row with exact evidence target and exact narrow evidence-chain wording. | `awk` acceptance confirms exactly one verified wall-seam row with target `//packages/parity:prusaslicer_wall_seam_parity`. |
| T-65-03-02 | Repudiation | High | `generated-outputs` row | mitigate | Verifiers require exactly one `generated-outputs` row with status `in progress`. | `bazel run //packages/prusa-wall-seam-scope:verify` and fixture verifier pass after status publication. |
| T-65-03-03 | Tampering | High | sibling Prusa rows | mitigate | Verifiers require unchanged exact `fork.prusaslicer.gcode-output` and `fork.prusaslicer.arc-fitting` rows, and mutation tests reject widened wording. | New mutation cases fail on G-code and arc-fitting row widening. |
| T-65-03-04 | Information Disclosure | Medium | status publication | mitigate | Status row adds no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, sync behavior, release behavior, or printer-runtime exposure. | Exact row wording is limited to checked-in summary evidence and deferrals. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel test //packages/parity:prusaslicer_wall_seam_parity_failure_test --test_output=errors
bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors
bazel test //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors
git diff --check -- packages/parity/status.tsv packages/parity-fixtures/verify_prusa_wall_seam_fixture.sh packages/parity-fixtures/verify_prusa_wall_seam_fixture_test.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
```
</verification>

<success_criteria>
SEAMEV-03 status publication is satisfied when exactly one narrow `fork.prusaslicer.wall-seam` row is verified, broad `generated-outputs` remains `in progress`, sibling Prusa rows remain exact, and fixture/scope verifier mutation suites reject status drift.
</success_criteria>

<output>
After completion, create `.planning/phases/65-executable-wall-seam-evidence/65-03-SUMMARY.md`.
</output>
