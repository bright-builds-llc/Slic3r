---
phase: 65-executable-wall-seam-evidence
plan: "05"
type: execute
wave: 5
depends_on: ["65-03", "65-04"]
files_modified:
  - packages/prusa-wall-seam-scope/README.md
  - packages/prusa-wall-seam-scope/wall-seam-scope.md
  - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
  - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
autonomous: true
requirements: [SEAMEV-03]
requirements_addressed: [SEAMEV-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-07-02T21-16-54
generated_at: 2026-07-02T21:32:54Z
must_haves:
  truths:
    - "Maintainer can read the wall-seam scope docs and see that Phase 65 published the public command/status row."
    - "Scope docs keep the Phase 62 scope contract historical boundary while removing stale Phase 65 planned/future wording."
    - "Scope verifier exact-text checks reject stale Phase 65 planned/future wording and preserve forbidden-claim guards."
  artifacts:
    - path: "packages/prusa-wall-seam-scope/wall-seam-scope.md"
      provides: "Published scope contract wording for Phase 65 wall-seam evidence"
      contains: "Phase 65 published `fork.prusaslicer.wall-seam`"
    - path: "packages/prusa-wall-seam-scope/README.md"
      provides: "Scope package overview with published public command/status wording"
      contains: "bazel run //packages/parity:prusaslicer_wall_seam_parity"
    - path: "packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh"
      provides: "Exact scope doc checks for published Phase 65 wording"
      contains: "Published narrow status row"
  key_links:
    - from: "packages/prusa-wall-seam-scope/wall-seam-scope.md"
      to: "packages/parity/status.tsv"
      via: "Published narrow status row traceability"
      pattern: "fork\\.prusaslicer\\.wall-seam"
    - from: "packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh"
      to: "packages/prusa-wall-seam-scope/wall-seam-scope.md"
      via: "require_section_table_row exact checks"
      pattern: "Published narrow status row"
---

<objective>
Update the wall-seam scope contract and scope verifier from Phase 65 planned wording to Phase 65 published wording.

Purpose: SEAMEV-03 requires public scope docs to reflect the exact narrow publication after the command, mutation guards, status row, and package docs exist, without widening the evidence boundary.
Output: Scope README, scope contract, and scope verifier/test checks that require published Phase 65 command/status wording and reject stale planned/future wording.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
@.planning/phases/65-executable-wall-seam-evidence/65-RESEARCH.md
@.planning/phases/65-executable-wall-seam-evidence/65-03-SUMMARY.md
@.planning/phases/65-executable-wall-seam-evidence/65-04-SUMMARY.md
@packages/parity/status.tsv
@packages/prusa-wall-seam-scope/README.md
@packages/prusa-wall-seam-scope/wall-seam-scope.md
@packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
@packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish scope docs for the Phase 65 wall-seam evidence row</name>
  <files>packages/prusa-wall-seam-scope/README.md, packages/prusa-wall-seam-scope/wall-seam-scope.md</files>
  <read_first>
    - packages/prusa-wall-seam-scope/README.md
    - packages/prusa-wall-seam-scope/wall-seam-scope.md
    - packages/parity/status.tsv
    - packages/parity/README.md
    - packages/parity-fixtures/README.md
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <action>Update `packages/prusa-wall-seam-scope/README.md` so the package overview says Phase 62 recorded the scope contract and Phase 65 now publishes `bazel run //packages/parity:prusaslicer_wall_seam_parity` and `fork.prusaslicer.wall-seam` for checked-in wall-seam summary evidence only. In `packages/prusa-wall-seam-scope/wall-seam-scope.md`, replace the Scope Record row `Planned evidence command` with `Public evidence command` and value `Phase 65 published \`bazel run //packages/parity:prusaslicer_wall_seam_parity\`; Phase 62 only planned the target and did not create it.` Replace the Scope Record row `Planned status token` with `Published narrow status row` and value `Phase 65 published \`fork.prusaslicer.wall-seam\` after executable evidence passed; Phase 62 only planned the row and did not publish it.` In the Wall-Seam Traceability table, replace `Planned public evidence command` with `Public evidence command` and value `` `bazel run //packages/parity:prusaslicer_wall_seam_parity` ``. Replace `Planned narrow status token` with `Published narrow status row` and value `Phase 65 published \`fork.prusaslicer.wall-seam\` as the narrow v1.16 checked-in wall-seam summary evidence slice backed by the Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public parity command.` Rename `## Planned Status Wording` to `## Published Status Wording`. In that section, use the sentence `The Phase 65 published status token is \`fork.prusaslicer.wall-seam\`.` followed by `Phase 65 published \`fork.prusaslicer.wall-seam\` as the narrow v1.16 checked-in wall-seam summary evidence slice backed by the Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public parity command.` Keep `generated-outputs` as `in progress`, keep `fork.prusaslicer.gcode-output` limited to the existing semantic Prusa G-code evidence slice, keep `fork.prusaslicer.arc-fitting` limited to the existing arc-fitting evidence slice, and keep every deferred term from D-10/D-12. Do not add UI, runtime, release, sync, network/device, host upload, upstream source import, printer-runtime, geometry-equivalence, printability, or non-Prusa support claims. This is the scope-docs step in the D-14/D-15 sequence after 65-04 package/fixture docs pass and before 65-06 public port docs. This implements D-12 and D-13 for scope docs.</action>
  <verify>
    <automated>mdformat --check packages/prusa-wall-seam-scope/README.md packages/prusa-wall-seam-scope/wall-seam-scope.md</automated>
    <automated>bazel run //packages/prusa-wall-seam-scope:verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Public evidence command|Published narrow status row|Published Status Wording' packages/prusa-wall-seam-scope/wall-seam-scope.md` returns matches for all three labels.
    - `rg -n 'Phase 65 published `fork.prusaslicer.wall-seam` as the narrow v1.16 checked-in wall-seam summary evidence slice backed by the Phase 62 scope contract, Phase 63 fixture corpus, Phase 64 Rust parser/readiness boundary, and Phase 65 public parity command\\.' packages/prusa-wall-seam-scope/wall-seam-scope.md` returns a match.
    - `rg -n 'bazel run //packages/parity:prusaslicer_wall_seam_parity|fork.prusaslicer.wall-seam' packages/prusa-wall-seam-scope/README.md packages/prusa-wall-seam-scope/wall-seam-scope.md` returns matches in both files.
    - `rg -n 'generated-outputs.*in progress|fork.prusaslicer.gcode-output.*semantic|fork.prusaslicer.arc-fitting.*arc-fitting' packages/prusa-wall-seam-scope/wall-seam-scope.md` returns matches.
    - `rg -n 'Planned evidence command|Planned status token|Planned public evidence command|Planned narrow status token|## Planned Status Wording|Phase 65 planned' packages/prusa-wall-seam-scope/README.md packages/prusa-wall-seam-scope/wall-seam-scope.md` returns no matches.
  </acceptance_criteria>
  <done>Scope docs publish the Phase 65 command/status row, retain historical Phase 62 boundaries, preserve deferred surfaces, and contain no stale Phase 65 planned/future wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align scope verifier exact-text checks with published scope docs</name>
  <files>packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh, packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh</files>
  <read_first>
    - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh
    - packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
    - packages/prusa-wall-seam-scope/README.md
    - packages/prusa-wall-seam-scope/wall-seam-scope.md
    - packages/parity/status.tsv
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <behavior>
    - RED: The scope verifier still accepts or requires Phase 65 planned/future wording before this task.
    - GREEN: The scope verifier requires published Phase 65 command/status wording and rejects stale planned/future wording.
  </behavior>
  <action>Update `verify_prusa_wall_seam_scope.sh` so `verify_scope_record` requires the exact Scope Record rows from Task 1: `Public evidence command` with value `Phase 65 published \`bazel run //packages/parity:prusaslicer_wall_seam_parity\`; Phase 62 only planned the target and did not create it.` and `Published narrow status row` with value `Phase 65 published \`fork.prusaslicer.wall-seam\` after executable evidence passed; Phase 62 only planned the row and did not publish it.` Update `verify_traceability_record` to require `Public evidence command` and `Published narrow status row` with the exact values from Task 1. Replace any section constant or section check for `## Planned Status Wording` with `## Published Status Wording`. Add explicit `reject_text` checks for these stale strings in `wall-seam-scope.md`: `Planned evidence command`, `Planned status token`, `Planned public evidence command`, `Planned narrow status token`, `## Planned Status Wording`, and `Phase 65 planned \`fork.prusaslicer.wall-seam\``. Update `verify_prusa_wall_seam_scope_test.sh` valid fixtures to the new rows and section. Add one focused stale-scope-doc mutation that replaces `Published narrow status row` with `Planned narrow status token` and asserts the diagnostic mentions `Published narrow status row`; add one focused stale-section mutation that replaces `## Published Status Wording` with `## Planned Status Wording` and asserts the diagnostic mentions `Published Status Wording`. Keep existing inventory, status-row, deferred-scope, local-only, and forbidden-claim mutation coverage.</action>
  <verify>
    <automated>bash -n packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh</automated>
    <automated>shfmt -l -d packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh</automated>
    <automated>bazel run //packages/prusa-wall-seam-scope:verify</automated>
    <automated>bazel test --cache_test_results=no //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Published narrow status row|Published Status Wording|Phase 65 published' packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh` returns matches in both files.
    - `rg -n 'Planned evidence command|Planned status token|Planned public evidence command|Planned narrow status token|## Planned Status Wording|Phase 65 planned `fork.prusaslicer.wall-seam`' packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh` returns no matches.
    - `bazel run //packages/prusa-wall-seam-scope:verify` exits 0.
    - `bazel test --cache_test_results=no //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test --test_output=errors` exits 0.
  </acceptance_criteria>
  <done>Scope verifier exact-text checks enforce published Phase 65 scope docs and mutation tests reject stale planned/future wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scope docs -> maintainer interpretation | Scope docs explain what the public wall-seam evidence proves and does not prove. |
| scope docs -> scope verifier | Exact checks decide whether scope docs are stale or overclaiming. |
| status row -> scope docs | Scope docs must describe the checked-in `fork.prusaslicer.wall-seam` row without promoting broad generated-output status or widening sibling rows. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification Criteria |
|-----------|----------|----------|-----------|-------------|-----------------|-----------------------|
| T-65-05-01 | Spoofing | High | scope docs | mitigate | Require exact `bazel run //packages/parity:prusaslicer_wall_seam_parity` and `fork.prusaslicer.wall-seam` wording tied to checked-in wall-seam summary evidence only. | `bazel run //packages/prusa-wall-seam-scope:verify` exits 0 and grep acceptance confirms published wording. |
| T-65-05-02 | Tampering | High | scope verifier | mitigate | Add exact-text checks and mutation tests that reject stale planned/future wording. | `bazel test --cache_test_results=no //packages/prusa-wall-seam-scope:verify_prusa_wall_seam_scope_test --test_output=errors` exits 0. |
| T-65-05-03 | Repudiation | High | published evidence boundary | mitigate | Preserve Phase 62-65 chain wording and explicit deferrals for byte parity, generated-output parity, geometry equivalence, printability, runtime, GUI, release, sync, network/device, host upload, upstream import, printer-runtime, and non-Prusa behavior. | Acceptance grep confirms deferral and sibling-row separation wording. |
| T-65-05-04 | Information Disclosure | Medium | scope docs | mitigate | Docs state no secrets, credentials, private data, network/device behavior, upstream source import, runtime discovery, host upload, sync behavior, release behavior, or printer-runtime exposure is introduced. | Existing forbidden-claim and deferred-scope checks remain active. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
bazel run //packages/parity:prusaslicer_wall_seam_parity
bazel run //packages/parity:prusaslicer_gcode_output_parity
bazel run //packages/parity:prusaslicer_arc_fitting_parity
bazel run //packages/parity-fixtures:verify_prusa_wall_seam_fixture
bazel run //packages/prusa-wall-seam-scope:verify
git diff --check -- packages/prusa-wall-seam-scope/README.md packages/prusa-wall-seam-scope/wall-seam-scope.md packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope.sh packages/prusa-wall-seam-scope/verify_prusa_wall_seam_scope_test.sh
```
</verification>

<success_criteria>
SEAMEV-03 scope documentation is satisfied when the scope README/contract and verifier publish the Phase 65 command/status row, reject stale planned/future wording, keep broad `generated-outputs` in progress, preserve sibling row meanings, and preserve all forbidden-claim guards.
</success_criteria>

<output>
After completion, create `.planning/phases/65-executable-wall-seam-evidence/65-05-SUMMARY.md`.
</output>
