---
phase: 65-executable-wall-seam-evidence
plan: "02"
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - packages/parity/compare_prusaslicer_wall_seam_test.sh
  - packages/parity/BUILD.bazel
autonomous: true
requirements: [SEAMEV-02]
requirements_addressed: [SEAMEV-02]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 65-2026-07-02T21-16-54
generated_at: 2026-07-02T21:32:54Z
must_haves:
  truths:
    - "Maintainer can run bazel test //packages/parity:prusaslicer_wall_seam_parity_failure_test --test_output=errors."
    - "Public mutation coverage rejects every D-06 wall-seam drift class through the public Rust-backed command path."
    - "Mutation tests edit only temp copies and leave checked-in expected-wall-seam-summary.tsv unchanged."
  artifacts:
    - path: "packages/parity/compare_prusaslicer_wall_seam_test.sh"
      provides: "Public wall-seam mutation guard suite"
      contains: "assert_wall_seam_value_mutation_fails"
    - path: "packages/parity/BUILD.bazel"
      provides: "prusaslicer_wall_seam_parity_failure_test sh_test target"
      contains: "name = \"prusaslicer_wall_seam_parity_failure_test\""
  key_links:
    - from: "packages/parity/compare_prusaslicer_wall_seam_test.sh"
      to: "packages/parity/compare_prusaslicer_wall_seam.sh"
      via: "public comparator invocation"
      pattern: "compare_prusaslicer_wall_seam.sh"
    - from: "packages/parity/compare_prusaslicer_wall_seam_test.sh"
      to: "packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv"
      via: "temp-copy mutation source"
      pattern: "expected-wall-seam-summary.tsv"
---

<objective>
Add fail-closed public mutation guards for SEAMEV-02.

Purpose: The public wall-seam command must fail on named wall-seam fact drift and unsupported deferred-behavior claims without duplicating Rust validation logic in Bash.
Output: `compare_prusaslicer_wall_seam_test.sh` and Bazel test target `//packages/parity:prusaslicer_wall_seam_parity_failure_test`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
@.planning/phases/65-executable-wall-seam-evidence/65-RESEARCH.md
@.planning/phases/65-executable-wall-seam-evidence/65-01-SUMMARY.md
@packages/parity/compare_prusaslicer_wall_seam.sh
@packages/parity/compare_prusaslicer_arc_fitting_test.sh
@packages/parity/BUILD.bazel
@packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv

<interfaces>
The comparator created by Plan 65-01 accepts:
```text
summary_binary rust-wall-seam-input.tsv expected-wall-seam-summary.tsv fixture-provenance.tsv
```

Every mutation test must call `compare_prusaslicer_wall_seam.sh`; do not re-implement the wall-seam validity decision in Bash. Bash may mutate one TSV field in a temp file, reorder rows in a temp file, and assert stderr names `expected-wall-seam-summary.tsv` plus the mutated field or boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wall-seam-specific temp-copy mutation cases</name>
  <files>packages/parity/compare_prusaslicer_wall_seam_test.sh</files>
  <read_first>
    - NEW FILE: packages/parity/compare_prusaslicer_wall_seam_test.sh
    - packages/parity/compare_prusaslicer_wall_seam.sh
    - packages/parity/compare_prusaslicer_arc_fitting_test.sh
    - packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <behavior>
    - RED: `rg -n 'seam_transition_observations' packages/parity/compare_prusaslicer_wall_seam_test.sh` fails before the test file exists.
    - GREEN: Each mutation copies `expected-wall-seam-summary.tsv` to a temp path whose basename remains `expected-wall-seam-summary.tsv`, mutates one field or row order, invokes `compare_prusaslicer_wall_seam.sh`, and asserts nonzero exit.
    - Guard: checked-in `expected-wall-seam-summary.tsv` still contains the exact original rows after the test run.
  </behavior>
  <action>Create `packages/parity/compare_prusaslicer_wall_seam_test.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`, per D-05. Follow `compare_prusaslicer_arc_fitting_test.sh`: compute `workspace_root` from Bazel `TEST_SRCDIR/TEST_WORKSPACE` or repo root, define `comparator`, resolve `summary_binary` from `packages/slic3r-rust/crates/slic3r_flavors/prusa_wall_seam_summary` or `bazel-bin/packages/slic3r-rust/crates/slic3r_flavors/prusa_wall_seam_summary`, set `fixture_dir`, `expected_wall_seam_summary`, and `fixture_provenance`, create `tmp_dir`, and clean it with `trap`. Add helpers `fail`, `assert_contains`, `assert_executable`, `assert_file`, `mutate_wall_seam_value(path, wall_seam_field, replacement)`, `mutate_wall_seam_boundary(path, wall_seam_field, replacement)`, `move_wall_seam_row_before(path, moved_field, before_field)`, `run_comparator(expected_artifact, stdout_file, stderr_file)`, `assert_wall_seam_value_mutation_fails(wall_seam_field, replacement)`, `assert_wall_seam_boundary_mutation_fails(wall_seam_field, replacement, required_diagnostic)`, and `assert_wall_seam_row_order_mutation_fails(moved_field, before_field)`. Add one value mutation for each D-06 field with these replacements: `seam_transition_observations -> seam_markers:seam_start;transition_count:1`, `layer_context_observations -> layer_index:1;z_values:0.300`, `travel_context_observations -> travel_moves:2;travel_from:12.500,8.000;travel_to:14.000,8.750`, `coordinate_bounds -> x_min:12.500;x_max:15.500;y_min:8.000;y_max:9.500;z_min:0.200;z_max:0.200`, `extrusion_observations -> e_values:0.12000,0.32000;e_axis_observed:true`, `retraction_observations -> e_marker_values:0.28000,0.20000;retraction_marker_observed:true`, `source_ref -> prusaslicer:version_2.9.5@0000000000000000000000000000000000000000`, `fixture_id -> wall-seam-unreviewed.gcode`, and `fixture_path -> packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/unreviewed.gcode`. Add one row-order mutation that moves `retraction_observations` before `extrusion_observations` and asserts stderr contains `retraction_observations`. Add one boundary mutation: mutate `seam_transition_observations` evidence boundary to `full generated-output parity verified` and assert stderr contains `full generated-output parity`. For every failed mutation, assert stderr contains `expected-wall-seam-summary.tsv` and the mutated field or required diagnostic, per D-08. After all cases, assert the checked-in summary still contains exact original lines for `seam_transition_observations`, `travel_context_observations`, `coordinate_bounds`, `extrusion_observations`, `retraction_observations`, `fixture_id`, and `fixture_path`. Print `ok: prusaslicer_wall_seam_parity_failure_test`. This implements D-05, D-06, D-07, and D-08.</action>
  <verify>
    <automated>bash -n packages/parity/compare_prusaslicer_wall_seam_test.sh</automated>
    <automated>shfmt -l -d packages/parity/compare_prusaslicer_wall_seam_test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'assert_wall_seam_value_mutation_fails' packages/parity/compare_prusaslicer_wall_seam_test.sh` returns a match.
    - `rg -n 'seam_transition_observations|layer_context_observations|travel_context_observations|coordinate_bounds|extrusion_observations|retraction_observations|source_ref|fixture_id|fixture_path' packages/parity/compare_prusaslicer_wall_seam_test.sh` returns matches for all listed fields.
    - `rg -n 'assert_wall_seam_row_order_mutation_fails|retraction_observations' packages/parity/compare_prusaslicer_wall_seam_test.sh` returns matches.
    - `rg -n 'full generated-output parity verified' packages/parity/compare_prusaslicer_wall_seam_test.sh` returns a match.
    - `rg -n 'expected-wall-seam-summary.tsv' packages/parity/compare_prusaslicer_wall_seam_test.sh` returns a match.
  </acceptance_criteria>
  <done>The mutation test script exists, covers all SEAMEV-02 / D-06 drift classes, and invokes the public Rust-backed comparator for every failure assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the public mutation suite into Bazel and run regressions</name>
  <files>packages/parity/BUILD.bazel</files>
  <read_first>
    - packages/parity/BUILD.bazel
    - packages/parity/compare_prusaslicer_wall_seam_test.sh
    - packages/parity/compare_prusaslicer_wall_seam.sh
    - packages/parity/compare_prusaslicer_arc_fitting_test.sh
    - .planning/phases/65-executable-wall-seam-evidence/65-CONTEXT.md
  </read_first>
  <action>Add `sh_test(name = "prusaslicer_wall_seam_parity_failure_test", srcs = ["compare_prusaslicer_wall_seam_test.sh"], data = ["compare_prusaslicer_wall_seam.sh", "//packages/slic3r-rust/crates/slic3r_flavors:prusa_wall_seam_summary", "//packages/parity-fixtures:prusa_wall_seam_expected_wall_seam_summary", "//packages/parity-fixtures:prusa_wall_seam_provenance"])` to `packages/parity/BUILD.bazel` near the existing Prusa parity failure tests. Run the new test with `--cache_test_results=no` once so all mutation cases execute. Also run `bazel run //packages/parity:prusaslicer_wall_seam_parity`, `bazel run //packages/parity:prusaslicer_gcode_output_parity`, `bazel run //packages/parity:prusaslicer_arc_fitting_parity`, `bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors`, and `bazel test //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors` to prove D-04 sibling command/test regression coverage. This is the mutation-guard step in the D-14/D-15 sequence after 65-01 public command verification and before 65-03 status/verifier publication; keep verification local to Bazel and shell surfaces, not broad legacy, launcher, packaging, release, sync, or external network suites per D-17.</action>
  <verify>
    <automated>bazel query //packages/parity:prusaslicer_wall_seam_parity_failure_test</automated>
    <automated>bazel test --cache_test_results=no //packages/parity:prusaslicer_wall_seam_parity_failure_test --test_output=errors</automated>
    <automated>bazel run //packages/parity:prusaslicer_wall_seam_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_gcode_output_parity</automated>
    <automated>bazel run //packages/parity:prusaslicer_arc_fitting_parity</automated>
    <automated>bazel test //packages/parity:prusaslicer_gcode_output_parity_failure_test --test_output=errors</automated>
    <automated>bazel test //packages/parity:prusaslicer_arc_fitting_parity_failure_test --test_output=errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'name = "prusaslicer_wall_seam_parity_failure_test"' packages/parity/BUILD.bazel` returns a match.
    - `rg -n 'compare_prusaslicer_wall_seam_test.sh' packages/parity/BUILD.bazel` returns a match.
    - `bazel test --cache_test_results=no //packages/parity:prusaslicer_wall_seam_parity_failure_test --test_output=errors` exits 0.
    - `rg -n $'seam_transition_observations\\tseam transition observations\\tseam_markers:seam_start,seam_resume;transition_count:2' packages/parity-fixtures/forks/prusaslicer/prusaslicer.wall-seam/expected-wall-seam-summary.tsv` returns a match after the test run.
  </acceptance_criteria>
  <done>The Bazel mutation target is public, runs successfully, and regression commands prove the existing Prusa G-code output and arc-fitting command/test contracts still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in fixture -> temp mutation | Tests copy source evidence into temp files before mutation. |
| temp mutated TSV -> public comparator | Mutated files cross into the same public Rust-backed validation path maintainers run. |
| stderr diagnostics -> maintainer evidence | Failure messages must identify the changed artifact and field or boundary. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan | Verification Criteria |
|-----------|----------|----------|-----------|-------------|-----------------|-----------------------|
| T-65-02-01 | Tampering | High | mutation tests | mitigate | Mutate only temp copies under `tmp_dir`; assert original checked-in rows remain unchanged after tests. | Acceptance grep confirms original checked-in rows after `bazel test --cache_test_results=no //packages/parity:prusaslicer_wall_seam_parity_failure_test`. |
| T-65-02-02 | Repudiation | High | diagnostics | mitigate | Assert stderr contains `expected-wall-seam-summary.tsv` and the exact changed field or unsupported-claim phrase for every drift class. | Test helper `assert_contains` checks artifact and field diagnostics for all D-06 cases. |
| T-65-02-03 | Elevation of Privilege / Information Disclosure | High | test harness temp paths | mitigate | Use only local Bazel runfiles and temp files; do not call Git, network, source import, slicer runtime, host upload, device, release, or sync commands. | `rg -n 'curl |git |clone|fetch|host upload|send-gcode|PrusaSlicer --|slic3r --' packages/parity/compare_prusaslicer_wall_seam_test.sh` returns no matches except split literal guards if added. |
</threat_model>

<verification>
Run the task verification commands, then run:

```bash
git diff --check -- packages/parity/compare_prusaslicer_wall_seam_test.sh packages/parity/BUILD.bazel
```
</verification>

<success_criteria>
SEAMEV-02 is satisfied when the public mutation test target fails closed on seam-transition observations, layer context, travel context, coordinate bounds, extrusion observations, retraction observations, source identity, fixture identity/path, row order, and unsupported deferred-behavior claim text.
</success_criteria>

<output>
After completion, create `.planning/phases/65-executable-wall-seam-evidence/65-02-SUMMARY.md`.
</output>
